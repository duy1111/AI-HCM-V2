/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['localhost', '0.0.0.0'], // Thêm '0.0.0.0' vào danh sách domains
    },
  }
  
  module.exports = nextConfig